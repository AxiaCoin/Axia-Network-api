{{> header }}

{{{ imports }}}

declare module '@axia-js/rpc-core/types.jsonrpc' {
  export interface RpcInterface {
    {{#each modules}}
    {{{name}}}: {
      {{#each items}}
      {{> docs}}
      {{{name}}}: AugmentedRpc<{{#if generic}}<{{{generic}}}>{{/if}}({{{args}}}) => Observable<{{{type}}}>>;
      {{/each}}
    };
    {{/each}}
  }
}
{{> footer }}
